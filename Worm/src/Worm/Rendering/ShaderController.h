#pragma once

#include "Shader.h"
#include "Worm/Core/Core.h"

#include <string>
#include <map>

namespace Worm {
	//class ShaderController {
	//public:
	//	static Shared<Shader> CreateShader(std::string vertex, std::string fragment) {
	//		// check which api is calling for a shader and return the correct type
	//	}

	//	static Shared<Shader> GetShader(std::string id) {
	//		return s_Shaders[id];
	//	}
	//private:
	//	static std::map<std::string, Shared<Shader>> s_Shaders;
	//};
}